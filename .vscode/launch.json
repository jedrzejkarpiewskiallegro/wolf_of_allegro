{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug wolf_of_allegro (balanced)",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "args": ["--scenario", "1", "--max-iterations", "45"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}